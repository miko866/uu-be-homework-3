'use strict';

const AUTH_MODE = {
  isAdmin: 'isAdmin',
  isSamePersonOrAdmin: 'isSamePersonOrAdmin',
  isOwner: 'isOwner',
  isAllowed: 'isAllowed'
};

const ROLE = {
  admin: 'admin',
  user: 'user',
};

module.exports = {
  AUTH_MODE,
  ROLE,
};
